Due to lack of task material (the PSD file was missing), so I decided to put my own design on the demo with Bulma CSS as an example.

Please execute the .sql file before running both the frontend and backend, and also don't forget to change the database connection credentials
according to your local machine (backend/config/database.js).

available endpoints:
//PUBLIC:
localhost:3000/register (Candidate Registration)
localhost:3000/login (Candidate Login)

//PANELS:
localhost:3000/panel/candidates (Candidate Registration)
localhost:3000/panel/teams (Candidate Registration)
